# Local dev — Vite proxy handles /api when VITE_API_URL is unset
VITE_API_URL=http://localhost:5001

# GitHub Pages subpath (must match repo name unless using a custom domain)
VITE_BASE_PATH=/guru-crm/

VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
