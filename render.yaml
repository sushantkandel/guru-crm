# Optional Blueprint — manual setup is easier; see RENDER_SETUP.md
services:
  - type: web
    name: guru-crm-api
    runtime: node
    plan: free
    rootDir: crm-app/backend
    buildCommand: npm install && npm run db:migrate:deploy
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://sushantkandel.github.io/guru-crm
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM
        sync: false
      - key: NOMINATIM_USER_AGENT
        value: GuruCRM/1.0 (admin@crm.com)
